# Frontend (Vite + React + Tailwind)

## Setup

From `frontend/`:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

## Assets

For the hero image, put your file here:

- `frontend/public/assets/images/portrait.png`

Then it loads at:

- `/assets/images/portrait.png`
